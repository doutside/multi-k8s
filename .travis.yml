sudo: required
services:
  - docker
before_install:
  - openssl aes-256-cbc -K $encrypted_0c35eebf403c_key -iv $encrypted_0c35eebf403c_iv -in service-account.json.enc -out service-account.json -d
  - curl https://sdk.cloud.google.com | bash > /dev/null;
  - source $HOME/google-cloud-sdk/path.bash.inc
  - gclound components update kubectl
  #- gcloud auth activate-service-acount --key-file service-account.json
  #- gcloud config set project verdant-inquiry-251611
  #- gcloud config set compute/zone europe-west4-b
  #- gcloud container clusters get-credentials multi-cluster
  #- echo "$DOCKER_PQSSWORD" | docker -u "$DOCKER_USERNAME" --password-stdout